<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reconinstruments.alarmclock"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>
	 <uses-permission android:name="android.permission.READ_SETTINGS"/>
	 <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AlarmClockActivity"
            android:label="@string/title_activity_alarm_clock" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AlarmSettingActivity"
      		android:label="@string/title_activity_alarm_clock" >
        </activity>
        <activity
            android:name=".AlarmPopup"
            android:label="@string/title_activity_alarm_clock"
            android:theme="@android:style/Theme.Translucent" >
        </activity>
        <service android:name=".AlarmClockService" />
        <service android:name=".AlarmClockService2" />
        <service android:name=".AlarmClockService3" />
        <service android:name="AlarmStartupService" />
        <receiver android:name="AlarmBootIntentReceiver">
            <intent-filter >
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>