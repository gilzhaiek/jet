package android.limopm;
// The functionality below is defined on limo side here we just put place holders
public class LimoPMNative {
    public native static final boolean SetPowerMode(int pm);
    public native static final boolean SetFreq(String freq);
}
